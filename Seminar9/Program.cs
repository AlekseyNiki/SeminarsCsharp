// Треугольник Паскаля
/*
Console.WriteLine("введите порядок степени");
int number = Convert.ToInt32(Console.ReadLine());

for (int i = 0; i < number; i++)
{
    for (int j = number; j > i + 1; j--)
    {
        Console.Write(" ");
    }

    int stepen = 1;

    for (int j = 0; j <= i; j++)
    {
        Console.Write(stepen + " ");
        stepen = (stepen * (i - j)) / (j + 1);
    }

    Console.WriteLine();
}
*/
//===============================================================================================